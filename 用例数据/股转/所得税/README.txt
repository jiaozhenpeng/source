1、当天转入的数据，报盘股东在系统中不存在的数据设置为挂账
2、如果存在虚拟股东需要查询TradingResult将根据knockNum查询到对应的主股东 更新到CustSellLimitTax表中，没有成交的，按主账户扣税
3、将未处理的数据和挂账的数据设置为正在处理中 过滤不存在的股东的数据
4、对于可用余额不足的  需要注意unFinishAmt的更新逻辑是否正确
5、成交记录在普通买卖中

注：当公参895=1时，不允许透支，当公参895=0时，允许透支